{
  "name": "@adobe/node-fetch-retry",
  "version": "1.0.1",
  "main": "index.js",
  "description": "Library for performing retries on HTTP requests",
  "author": {
    "name": "Adobe Inc."
  },
  "repository": "adobe/node-fetch-retry",
  "license": "Apache-2.0",
  "dependencies": {
    "node-fetch-npm": "^2.0.4"
  },
  "devDependencies": {
    "@adobe/eslint-config-asset-compute": "^1.2.0",
    "eslint": "^7.0.0",
    "get-port": "^5.1.1",
    "mocha": "^7.1.2",
    "nock": "^12.0.3",
    "nyc": "^15.0.1",
    "rewire": "^5.0.0"
  },
  "keywords": [
    "fetch",
    "retry",
    "adobe",
    "http"
  ],
  "scripts": {
    "test": "nyc --reporter=html --reporter=text ./node_modules/.bin/mocha ./test --recursive --exit",
    "posttest": "eslint ./",
    "dep-license-details": "license-checker -unknown --out dependencies-licenses.log",
    "dep-license-list": "license-checker --summary -unknown"
  }
}
